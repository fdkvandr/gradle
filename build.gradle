plugins {
    id 'java'
}

repositories {
    mavenCentral()
}

configurations {
    Configuration configurationImpl = getByName('implementation')
    DependencySet implDependencies = configurationImpl.getDependencies()

    myConfiguration {
        description "my configuration"
    }
    configurationImpl.extendsFrom(myConfiguration)
}

dependencies {
    myConfiguration 'org.springframework:spring-webmvc:5.3.24'
}


apply from: 'script.gradle'

